<%i = 1%>
<table class = "table table-bordered">
<%@answers.each do |answer|%>
    <tr>
	    <td><%= i%></td>
	    <td><%= User.find(answer.student_id).name%></td>
	    <td><%= answer.content%></td>
	    <td><%= link_to "Открыть ответ", answer%></td>
    </tr>
<%end%>
</table>