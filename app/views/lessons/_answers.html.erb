<%i = 1%>
<table class = "table table-bordered">
<%@answers.each do |answer|%>
    <tr>
	    <td><%= i%></td>
	    <td><%= User.find(answer.student_id).name%></td>
	    <td><%= answer.content%></td>
	    <td><%= link_to "Открыть файл", answer.answer_file.url%></td>
    </tr>
<%end%>
</table>